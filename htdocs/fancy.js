    $(function () {
        var img = new Image();
        $(img).load(function () {
            $(this).hide();
            $('#loader1').removeClass('loading').append(this);
            $(this).fadeIn();
        }).error(function () {
            // notify the user that the image could not be loaded
        }).attr('src', 'images/plotter.php?var=td');
    });

    $(function () {
        var img = new Image();
        $(img).load(function () {
            $(this).hide();
            $('#loader2').removeClass('loading').append(this);
            $(this).fadeIn();
        }).error(function () {
            // notify the user that the image could not be loaded
        }).attr('src', 'images/plotter.php');
    });

    $(function () {
        var img = new Image();
        $(img).load(function () {
            $(this).hide();
            $('#loader3').removeClass('loading').append(this);
            $(this).fadeIn();
        }).error(function () {
            // notify the user that the image could not be loaded
        }).attr('src', 'images/plotter.php');
    });

    $(function () {
        var img = new Image();
        $(img).load(function () {
            $(this).hide();
            $('#loader4').removeClass('loading').append(this);
            $(this).fadeIn();
        }).error(function () {
            // notify the user that the image could not be loaded
        }).attr('src', 'images/plotter.php');
    });

    $(function () {
        var img = new Image();
        $(img).load(function () {
            $(this).hide();
            $('#loader5').removeClass('loading').append(this);
            $(this).fadeIn();
        }).error(function () {
            // notify the user that the image could not be loaded
        }).attr('src', 'images/plotter.php');
    });

    $(function () {
        var img = new Image();
        $(img).load(function () {
            $(this).hide();
            $('#loader6').removeClass('loading').append(this);
            $(this).fadeIn();
        }).error(function () {
            // notify the user that the image could not be loaded
        }).attr('src', 'images/plotter.php');
    });

    $(function () {
        var img = new Image();
        $(img).load(function () {
            $(this).hide();
            $('#loader7').removeClass('loading').append(this);
            $(this).fadeIn();
        }).error(function () {
            // notify the user that the image could not be loaded
        }).attr('src', 'images/plotter.php');
    });

    $(function () {
        var img = new Image();
        $(img).load(function () {
            $(this).hide();
            $('#loader8').removeClass('loading').append(this);
            $(this).fadeIn();
        }).error(function () {
            // notify the user that the image could not be loaded
        }).attr('src', 'images/plotter.php');
    });

